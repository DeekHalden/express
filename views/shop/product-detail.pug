extends ../base.pug
block content
  main.centered
    h1!=product.title
    div
      img(
        src!=product.imageUrl,
        alt!=product.title
      )
    h2!=product.price
    p!=product.description
    include ./../includes/add-to-cart



